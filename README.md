# gss

#### A small bash script to switch git branches with less friction.

**gss** stashes your work, switches the branch, syncs with the remote, and can re-apply your last stash for that branch.

### Usage

```bash
gss <branch-name>
```
